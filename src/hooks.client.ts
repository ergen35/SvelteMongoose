// import type { Handle } from '@sveltejs/kit';

// export const handle = (async ({ event, resolve }) => {

//     console.log("Handled client side request");

//     const response = await resolve(event);
//     return response;

// }) satisfies Handle;

console.log("Handled client side request");